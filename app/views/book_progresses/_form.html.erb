<%= form_for(@book_progress) do |f| %>
  <% if @book_progress.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book_progress.errors.count, "error") %> prohibited this book_progress from being saved:</h2>

      <ul>
      <% @book_progress.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :percent %><br>
    <%= f.number_field :percent %>
  </div>
  <div class="field">
    <%= f.label :book_id %><br>
    <%= f.number_field :book_id %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.datetime_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
